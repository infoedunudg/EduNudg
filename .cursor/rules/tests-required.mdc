---
description: Tests required for every feature and bugfix
alwaysApply: true
---

# Tests Required

## Policy

No feature or bugfix merges without tests in the same change.

| Change type | Required tests |
|-------------|----------------|
| Schema / RLS | `supabase/tests/*.sql` |
| Package logic | Vitest `*.test.ts` |
| UI feature | Vitest + Testing Library; Playwright for journeys |
| Bugfix | Regression test named `regression_*` |
| Edge Function + SPA client | Vitest for client wrapper; RLS if migration adds RPC |

## Commands

- `pnpm test` — Vitest
- `pnpm test:rls` — Supabase RLS suite
- `pnpm test:e2e` — Playwright

Use skill: `.cursor/skills/edunudg-write-tests/SKILL.md`

Before finish: `.cursor/skills/edunudg-sync-artifacts/SKILL.md` (specs, docs, tests, skills, agents).

## Locators

- **Playwright**: `getByRole({ name })` is substring match — use `{ exact: true }` for shared prefixes (`Log in` vs `Log in with Google`; `Email` vs `Login email (optional)`). Center `/app/leads` list titles use parent name (`leadDisplayName`), not `child_name`.
- **Testing Library**: do **not** pass `exact: true` (not on `ByRoleOptions`; breaks typecheck). Use `exactAccessibleName("…")` from `@/test/exactAccessibleName` (anchored `/^…$/`).
- See `openspec/specs/staff-login/spec.md`. Staff franchise `/login` MUST keep `regression_does_not_sign_out_before_memberships_fetch_completes`, `regression_accepts_invited_memberships_before_loading_active_rows`, and `regression_center_login_keeps_portal_query_on_app_redirect` green. Brand `/login` MUST keep `regression_brand_does_not_sign_out_before_memberships_fetch_completes` green. Learn `/login` MUST keep `regression_learn_portal_does_not_sign_out_without_staff_membership` green.

## Platform homepage regressions

Bugfixes or changes to `isLegacyPlatformHomepageSeed`, brand/center landing merges, or homepage defaults MUST keep `regression_homepageLegacySeedKeepsBrandAssets` and `regression_tenantMarketingContentPreservesBrandAssets` green and follow rule `marketing-homepage-media`. Platform `/login` MUST keep `regression_login_renders_platform_nav_and_footer` and `regression_platform_login_renders_marketing_nav_and_footer` green (same Site nav + enterprise footer as `/`). Brand `/login` MUST keep `regression_brand_login_renders_public_nav_and_footer` green (same public nav + footer as that brand `/`). Learn `/login` MUST keep `regression_learn_login_renders_franchise_nav_and_footer` green (franchise public nav + footer when the center is known). Pricing feature ticks MUST keep `regression_pricing_feature_checkmarks_use_css_unicode_escape` green (CSS `content` uses `\2713`, not a raw `✓`). Brand `/app` Center Health MUST keep `regression_center_health_is_100_only_when_all_setup_minimums_are_met`, `regression_center_health_lists_missing_setup_reasons`, `regression_center_health_includes_homepage_and_franchise_site_content`, `regression_center_health_login_popup_opens_when_setup_incomplete`, `regression_center_health_login_popup_css_is_centered_themed_and_responsive`, and `regression_skips_center_health_popup_when_setup_is_complete` green (six equal checks: curriculum / feedback / students / franchises / homepage / franchise site; centered themed login popup only when below 100%). Brand `/app/students` MUST keep `regression_brand_students_page_matches_centers_chrome`, `regression_brand_students_search_by_name_franchise_city`, `regression_brand_student_card_shows_contact_and_levels`, and `regression_brand_students_exports_full_roster_csv` green. Public brand/franchise nav logos MUST keep `regression_public_nav_logo_matches_franchise_size_without_border` green. Center `/app/students` **Copy Profile URL** MUST keep `regression_center_student_copies_learn_login_url_without_password` green. Franchise **Student Login** / `learnPortalLoginUrl` on Vercel MUST keep `regression_vercel_student_login_uses_path_before_portal_query` green (`/login?portal=learn&brand=…`, never `brand=…/login`). Learn profile **Center website** MUST keep `regression_vercel_center_website_rewrites_localhost_rpc_url` and `regression_vercel_profile_center_website_avoids_localhost_rpc_url` green (never raw `*.localhost:9000` from RPC). Brand `/app/centers` **Import Franchise** MUST keep `regression_franchise_import_accepts_xlsx` green (`.csv`, `.xlsx`, `.xls`) and `regression_franchise_csv_maps_owner_display_state_and_curriculum` green (`name` = Franchise Owner, `proposed_franchise_name` = Display Name, `state` = State, `curriculum_assignment`). The import template MUST keep `regression_franchise_import_template_uses_owner_name_header` green (`Owner Name` header, sample `Amit Sharma`). Server import failures MUST keep `regression_franchise_import_shows_server_row_errors` green (highlighted per-row copy, not a muted “N rows failed” note). Reimport of the same Franchise Owner name MUST restore a soft-deleted franchise via `import_franchise_centers` (`102_import_franchise_restore_deleted.sql`). Brand `/app/centers` **Export Franchise** MUST keep `regression_brand_centers_exports_full_franchise_csv` green. Brand `/app/centers` **Delete franchise** MUST keep `regression_brand_centers_confirm_delete_calls_soft_delete_rpc` green (confirmation dialog, not an inline section). Center `/app/students` **Save address** MUST keep `regression_save_address_shows_saved_status` green. Center `/app/inventory` and `/app/merchandise` Shop MUST keep `regression_center_inventory_shows_catalog_curriculum` and `regression_center_merchandise_shop_shows_catalog_curriculum` green. Center `/app/merchandise` Shop title/price MUST keep `regression_center_merchandise_shop_title_wraps_instead_of_overlapping_price` green. Inventory list cards MUST keep `regression_center_inventory_list_omits_detail_duplicates` green. Staff lead lists MUST keep `regression_lead_list_select_includes_csv_aligned_columns` green after `090_lead_csv_aligned_fields.sql`. Center `/app` staff chrome MUST keep `regression_center_shell_lockup_shows_franchise_by_brand` green (franchise name + “by {brand}” tagline next to the Site logo). Public franchise nav MUST keep `regression_franchise_nav_shows_center_then_by_brand` green. Spark Academy Features Last month overlay MUST keep `regression_spark_features_omits_view_all_float_action` green (no **View all →**). Spark Academy **Courses designed for success** MUST keep `regression_spark_courses_lists_all_published_programs` green (every published course is listed). Spark course cards MUST keep `regression_spark_course_card_links_to_public_detail` green (media/title open `/courses/:slug`; **Enroll now** stays a modal button). Spark `/courses/:slug` MUST keep `regression_spark_course_page_uses_courses_section_background` green (same `--sa-page` canvas as the courses section, not a plain white sheet). Spark course cards MUST keep `regression_spark_course_card_banner_shows_full_image` green (full uploaded banner, not a cropped cover). Public `/courses/:slug` MUST keep `regression_public_course_page_shows_curriculum_fields`, `regression_public_course_page_has_enroll_offer_card`, `regression_public_course_enroll_card_stays_sticky_while_scrolling`, `regression_public_course_level_expand_has_structured_panels`, and `regression_unknown_course_slug_redirects_home` green. Curriculum Course Banner preview MUST keep `regression_curriculum_banner_preview_fills_frame_without_side_gaps` green. Curriculum course banners MUST keep `regression_curriculum_banner_upload_uses_per_course_slot`, `regression_curriculum_course_banner_upload_does_not_share_brand_slot`, and `regression_curriculum_banner_stays_per_course_when_switching` green (per-course `brand-assets` folder; switching courses must not show another course’s banner). Spark Academy Journey YouTube MUST keep `regression_spark_journey_renders_youtube_below_photo` green (embed under Journey; photo stays). Spark Academy `/about` MUST keep `regression_spark_about_page_uses_homepage_section_blocks`, `regression_spark_about_hero_omits_homepage_badges`, and `regression_spark_about_features_omits_float_badges` green (Hero / Features / Journey / Mentors; no Mastermind-only About chrome; homepage badges stay on `/`). Spark **Meet Our Expert Mentors** MUST keep `regression_spark_mentor_card_shows_role_badge_and_title` green (Role badge and Title both render). Brand `/app/homepage` Mentors / Leadership MUST keep `regression_spark_mentors_default_to_one_example_profile`, `regression_spark_mentors_collapse_saved_stock_placeholders_to_one`, and `regression_spark_mentors_keep_custom_profiles_and_one_stock_example` green (one dummy example; add/update/delete unchanged). Brand `/app/homepage` and `/app/center-site` MUST keep `regression_spark_homepage_requires_first_photo_per_section`, `regression_spark_homepage_hero_photo_satisfies_about_hero_requirement`, `regression_required_photo_shows_asterisk_and_hint_when_empty`, `regression_unsaved_marketing_navigation_opens_dialog_and_scrolls`, `regression_unsaved_marketing_dialog_uses_website_theme_and_ok_cancel`, and `regression_unsaved_marketing_hook_does_not_require_data_router` green (first photo per section required; homepage Hero satisfies About hero; unsaved leave is themed OK/Cancel and scrolls to the bottom; BrowserRouter only). Brand/center public layouts MUST keep `regression_public_layout_does_not_fetch_dummy_brand_slug` and `regression_public_layout_does_not_fetch_dummy_center_slug` green (never fetch or submit with placeholder slugs `brand` / `center`). EduLearn **Meet our leadership** MUST keep `regression_edu_learn_mentor_card_shows_role_badge_and_title` green. Brand and franchise public testimonials MUST keep `regression_center_hides_dummy_testimonials_when_brand_has_no_stories`, `regression_center_shows_brand_success_stories_on_homepage`, `regression_brand_hides_dummy_testimonials_when_no_published_stories`, and `regression_spark_omits_testimonials_when_no_public_stories` green (published brand success stories only; hide `#testimonials` when none). Public SEO/AEO/GEO MUST keep `regression_public_seo_derives_title_description_canonical`, `regression_center_seo_localizes_city_and_does_not_canonicalize_to_brand`, `regression_private_and_preview_routes_are_noindex`, `regression_sitemap_lists_published_courses_not_login`, `regression_sitemap_omits_hash_sections_and_unpublished_about`, `regression_course_page_emits_course_json_ld`, `regression_faq_json_ld_omitted_when_no_public_faqs`, `regression_vercel_rewrites_do_not_swallow_robots_sitemap_llms`, `regression_vite_seo_plugin_does_not_statically_import_app_aliases`, `regression_vercel_login_is_not_rewritten_to_seo_document`, and `regression_vercel_seo_api_accepts_node_request_path` green (derived tags only; no SEO editor fields; robots/sitemap/llms before the SPA catch-all; Vite plugin must not import `@/` at config load; `/login` stays on `/index.html`).

